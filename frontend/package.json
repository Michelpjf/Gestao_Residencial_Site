{
  "name": "bueno-residence-frontend",
  "private": true,
  "scripts": {
    "lint": "node --check app.js && node --check src/shared/api-client.js && node --check src/shared/api-session.js",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run lint && npm test"
  }
}
